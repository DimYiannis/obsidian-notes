The model outputs logits (raw scores) over its entire vocabulary (~150k tokens) at every generation step.

Instead of blindly picking the highest score, we intervene before sampling.

The model sees the full context — system prompt with function definitions + user prompt — and its probability distribution naturally favours the semantically correct function and arguments. The constraint layer only controls the **form**, not the **meaning**.
