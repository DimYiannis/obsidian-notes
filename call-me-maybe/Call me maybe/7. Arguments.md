
constrain to valid JSON keys and correct types

LLM is doing the semantic understanding it knows "sum of 40 and 2" maps to an addition function.

Making sure it can only _express_ that understanding as a valid, schema-compliant JSON output.

The constraint is on the **form**, not the **meaning**. The LLM handles meaning, you handle structure.