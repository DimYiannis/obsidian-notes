The user provides a natural language request.

like `"What is the sum of 40 and 2?"`

This is the only input from the user. Everything else — function definitions, output format, schema constraints — is handled by the system. The prompt goes through `parser.py` where it is validated as a Pydantic `Prompt` model before entering the pipeline.
