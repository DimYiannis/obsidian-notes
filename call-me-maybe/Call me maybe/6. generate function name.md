
when it's time to generate the function name, the only tokens allowed are ones that spell out a valid function name from your list — the LLM's own probability distribution naturally favors `fn_add_numbers` because that's what makes sense given the prompt