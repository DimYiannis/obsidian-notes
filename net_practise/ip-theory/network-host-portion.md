# Network portion vs host portion

Every [[ip-address]] splits in two:
- **network part** — which network you're on
- **host part** — which specific device

The [[subnet-mask]] decides where the split falls. Addresses sharing the same network part form a [[subnet]].

Up: [[ip-theory]]
