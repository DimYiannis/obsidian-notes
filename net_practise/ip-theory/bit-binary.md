# Bit & binary

Computers store the address as **bits** — 0s and 1s. IPv4 = **32 bits** total. `192` in binary = `11000000`. The [[dotted-decimal]] form we read is a friendlier skin over binary.

8 bits group into one [[octet]].

Used by: [[ip-address]] · [[subnet-mask]] · [[cidr]]
Up: [[ip-theory]]
