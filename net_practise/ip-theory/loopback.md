# Loopback

`127.0.0.1` (a.k.a. `localhost`). Points back to your own machine. Used for testing — traffic never leaves the device.

A special [[ip-address]], see also [[reserved-ranges]].
Up: [[ip-theory]]
