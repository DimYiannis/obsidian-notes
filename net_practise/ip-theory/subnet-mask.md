# Subnet mask

A second 32-[[bit-binary|bit]] number marking which bits are **network** (1s) vs **host** (0s). `255.255.255.0` = first 24 bits network. Decides the [[network-host-portion]] split. Same idea as [[cidr|CIDR /24]].

Defines a [[subnet]].
Up: [[ip-theory]]
