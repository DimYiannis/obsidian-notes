# Port

A numbered door on an [[ip-address|IP]] for a specific service. `:80` web, `:443` HTTPS, `:22` SSH. IP finds the machine; port finds the app on it.

Lives in the [[protocol|TCP/UDP]] header.
Up: [[ip-theory]]
